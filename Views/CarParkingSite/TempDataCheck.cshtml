@model OnlineCarParkingBookingManagament.Entity.CarParkingBooking

@{
    ViewBag.Title = "TempDataCheck";
    Layout = "~/Views/Shared/HomePage.cshtml";
}

<h2>TempDataChecking</h2>
@Html.ActionLink("Create a new Parking site details", "Create")
<br />
<table border="1">

    @foreach (OnlineCarParkingBookingManagament.Entity.CarParkingBooking item in (List<OnlineCarParkingBookingManagament.Entity.CarParkingBooking>)TempData["carparkingBookings"])
    {
        <tr>
            <td>carId</td>
            <td>carParkingSiteName</td>
            <td>carParkingSiteLocation</td>
            <td>parkingSlots </td>
        </tr>
        <tr>
            <td>
                @item.carId
            </td>
            <td>
                @item.carParkingSiteName
            </td>
            <td>
                @item.carParkingSiteLocation
            </td>
            <td>
                @item.parkingSlots
            </td>
        </tr>
        <tr>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.carId })
                @Html.ActionLink("Delete", "Delete", new { id = item.carId })
            </td>
        </tr>
    }
</table>



