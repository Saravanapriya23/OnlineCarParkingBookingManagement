@model IEnumerable<OnlineCarParkingBookingManagament.Entity.CarParkingBooking>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/HomePage.cshtml";
}
<h2>Car</h2>
<table border="1">
    <tr>
        <td>CarParkingSiteId</td>
        <td>CarParkingSiteName</td>
        <td>CarParkingSiteLocation</td>
        <td>ParkingSlots</td>
    </tr>
    @foreach (OnlineCarParkingBookingManagament.Entity.CarParkingBooking item in @Model)
    {
        <tr>
            <td>
                @item.carId
            </td>
            <td>
                @item.carParkingSiteName
            </td>
            <td>
                @item.carParkingSiteLocation
            </td>
            <td>
                @item.parkingSlots
            </td>
        </tr>
    }
</table>

